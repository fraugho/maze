use const_random::const_random;

pub const WIDTH: usize = 50;
pub const HEIGHT: usize = 50;

//pub const WIDTH: usize = const_random!(u8) as usize;
//pub const HEIGHT: usize = const_random!(u8) as usize;
